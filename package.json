{
  "name": "mcp-ubiquiti",
  "version": "0.1.0",
  "description": "Remote MCP connector for UniFi — lets Claude read your network (clients, devices, stats, ISP) through Ubiquiti's cloud API. No local proxy or on-prem agent.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260831.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.128.0"
  }
}
